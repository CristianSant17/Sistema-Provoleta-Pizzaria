/**
 * PROVOLETA — Constantes globais
 * Altere aqui quando valores padrão da loja mudarem.
 */

/** WhatsApp padrão (DDD + número, sem código do país) */
export const DEFAULT_WHATSAPP_NUMBER = '7799368373';

/** WhatsApp padrão formatado para exibição */
export const DEFAULT_WHATSAPP_FORMATTED = '+55 77 9936-8373';

/** Arquivo JSON único da página pública (commitado no GitHub) */
export const PUBLIC_MENU_FILE = 'cardapio_publico.json';

/** Identificador do schema do cardápio público */
export const PUBLIC_MENU_SCHEMA = 'provoleta-public-menu';

/** Página do cliente (sem acesso ao admin) */
export const CLIENT_ORDER_PAGE = 'pedido.html';
